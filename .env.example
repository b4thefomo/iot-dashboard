# ===================
# BACKEND (Render)
# ===================

# Google Gemini API Key (required)
GEMINI_API_KEY=your_gemini_api_key_here

# Port (Render sets this automatically, default 4000 for local)
PORT=4000

# Allowed frontend origins for CORS (comma-separated)
# Example: https://your-app.vercel.app,http://localhost:4001
CORS_ORIGINS=http://localhost:4001

# Firmware upload token (for OTA updates)
# Generate with: openssl rand -hex 32
FIRMWARE_UPLOAD_TOKEN=your_firmware_token_here

# ===================
# FRONTEND (Vercel)
# ===================

# Backend API URL - set this in Vercel dashboard
# Example: https://iot-server.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:4000
